{
    "version": "0.2.0",
    "configurations": [
        
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Deno",
            "cwd": "${workspaceFolder}",
            "runtimeExecutable": "deno",
            "runtimeArgs": ["run", "--allow-net", "--inspect", "--allow-write", "--allow-read", "--unstable", "./index.ts", "new"]
        }
    ]
}